overrides:
  tableGroups:
    Github:
      name: Github
      tables:
        - Repository
        - PullRequest
        - GitHubSchemaFilePath
        - GitHubDocFilePath
      comment: Tables related to Github. All tables in this group should have a GitHub prefix.
  tables:
    Repository:
      comment: "TODO: Rename to GitHubRepository to match the GitHub prefix pattern of this table group"
    PullRequest:
      comment: "TODO: Rename to GitHubPullRequest to match the GitHub prefix pattern of this table group"
