/**
 * Please remove this code comment after implementation starts.
 * Story Guidelines:
 * - Ensure `pnpm test:storybook` passes in CI (renders correctly)
 *   - For components with GraphQL requests, set up msw handlers to avoid errors
 * - Test coverage is not a priority at the moment; it is sufficient to render only the default `Default`. However, developers may add stories at their discretion.
 */
import type { Meta, StoryObj } from '@storybook/react'

import { {{ pascalCase name }} } from './'

const meta = {
  component: {{ pascalCase name }},
  args: {

  }
} satisfies Meta<typeof {{ pascalCase name }}>

export default meta
type Story = StoryObj<typeof meta>

export const Default: Story = {}
